## Gemini Added Memories
- The user's system uses 'nerdctl' instead of 'docker'. Always use 'nerdctl' for container-related tasks.
- Always use dash-case for file names unless the project already has an established different conventiona.
- git commit messages should never be longer than 80 chars.
