dotfiles
--------

These are my dotfiles. There are many like them, but these are mine.

## Setup

Initialize and apply these dotfiles:

```bash
chezmoi init --apply stefanotorresi
```

If you don't have `chezmoi` installed:

```bash
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply stefanotorresi
```
